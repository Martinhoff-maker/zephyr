tests:
  boards.silabs.dma.block_append:
    depends_on: dma
    tags:
      - drivers
      - dma
    platform_allow:
      - silabs/radio_boards/slwrb4180a  
    filter: dt_alias_exists("dma0")
    integration_platforms:
      - silabs/radio_boards/slwrb4180a 
